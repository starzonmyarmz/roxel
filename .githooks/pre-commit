#!/usr/bin/env bash
# Fast gate: format only. Heavier `cargo test` runs in pre-push.
# Skip with `git commit --no-verify`.
set -e
cd "$(git rev-parse --show-toplevel)"
echo "pre-commit: cargo fmt --check"
cargo fmt --all -- --check
